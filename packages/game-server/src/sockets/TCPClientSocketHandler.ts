import { Socket } from "socket.io";

import SocketManager from "./SocketManager";

export default class TCPClientSocketHandler {
    public static handleLobby(socket: Socket, manager: SocketManager) {
        
    }

    public static handleBattle(socket: Socket, manager: SocketManager) {
        
    }
}