import { Dispatch } from "redux";

export const handle_socket_game_on_change_bombs = (dispatch: Dispatch, bombs: number) => {
   
};

export const handle_socket_game_on_change_speed = (dispatch: Dispatch, speed: number) => {

};

export const handle_socket_game_on_change_radius = (dispatch: Dispatch, radius: number) => {
    
};