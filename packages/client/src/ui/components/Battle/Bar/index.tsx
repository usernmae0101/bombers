import React from "react";
import styles from "./bar.module.scss";

const Bar = () => {
    return (
        <div className={styles.bar}>
            <span>(крестик)</span>
        </div>
    );
};

export default Bar;