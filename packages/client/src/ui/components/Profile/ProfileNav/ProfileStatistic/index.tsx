import React from "react";

const ProfileStatistic: React.FC<{
    nickname: string;
}> = ({ nickname }) => {
    return (
        <div>
            статистика
        </div>
    );
};

export default ProfileStatistic;
