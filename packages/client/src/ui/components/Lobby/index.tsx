import React from "react";

const Lobby = () => {
    React.useEffect(() => {
    }, []);

    return (
        <div className="lobby">
            <ul>
            </ul>
        </div>
    );
};

export default Lobby;